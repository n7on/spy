name: Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (without v prefix, e.g., 1.0.0)'
        required: false
        type: string

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Get version from tag
        id: version
        shell: pwsh
        run: |
          if ('${{ github.event.inputs.version }}') {
            $version = '${{ github.event.inputs.version }}'
          } else {
            $version = '${{ github.ref_name }}' -replace '^v', ''
          }
          Write-Output "VERSION=$version" >> $env:GITHUB_OUTPUT
          Write-Host "Publishing version: $version"

      - name: Update manifest version
        shell: pwsh
        run: |
          $manifestPath = 'src/DllSpy.PowerShell/DllSpy.psd1'
          $content = Get-Content $manifestPath -Raw
          $content = $content -replace "ModuleVersion = '[^']*'", "ModuleVersion = '${{ steps.version.outputs.VERSION }}'"
          Set-Content -Path $manifestPath -Value $content
          Write-Host "Updated manifest to version ${{ steps.version.outputs.VERSION }}"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore

      - name: Test
        run: dotnet test -c Release --no-build

      - name: Verify module loads
        shell: pwsh
        run: |
          Import-Module ./out/DllSpy -Force
          $commands = Get-Command -Module DllSpy
          if ($commands.Count -lt 2) {
            throw "Expected at least 2 cmdlets, got $($commands.Count)"
          }
          Write-Host "Module loaded successfully with $($commands.Count) cmdlets"

      - name: Pack CLI tool
        run: dotnet pack src/DllSpy.Cli/DllSpy.Cli.csproj -c Release -o nupkg

      - name: Verify package exists
        shell: pwsh
        run: Get-ChildItem -Path nupkg -Recurse

      - name: NuGet login
        uses: NuGet/login@v1
        id: nuget-login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Publish to NuGet
        shell: pwsh
        run: dotnet nuget push (Get-ChildItem nupkg/*.nupkg).FullName --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Publish to PowerShell Gallery
        shell: pwsh
        env:
          PSGALLERY_API_KEY: ${{ secrets.PSGALLERY_API_KEY }}
        run: |
          if (-not $env:PSGALLERY_API_KEY) {
            throw "PSGALLERY_API_KEY secret is not set"
          }

          Write-Host "Publishing DllSpy module to PowerShell Gallery..."
          Publish-Module -Path "./out/DllSpy" -NuGetApiKey $env:PSGALLERY_API_KEY -Repository PSGallery -Verbose

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          generate_release_notes: true
          files: |
            out/DllSpy/*.dll
            out/DllSpy/*.psd1
            nupkg/*.nupkg
